interface:
  display_name: "Author Prow AI Diagnostics"
  short_description: "Diagnose failures and author Prow AI guidance"
  default_prompt: "Use $author-prow-ai-diagnostics to diagnose a representative failure corpus, improve the consumer prompt, and propose recipes only for repeated prompt-only misses without activating them."
