**What this PR does and why**

**Which issue(s) this closes**

Fixes #

**Verification**

<!-- The checks you ran, e.g. `make test`, `go test ./internal/ai/... -count=1`. -->

**Release note**

<!--
Write this for someone upgrading Aster, not for a reviewer. Say what changed and
what it means for them. Multiple paragraphs are fine and often right: the
symptom, then the cause, then the fix. Call out breaking changes explicitly.

This block is published verbatim in the release notes, so write it as finished
prose rather than as a summary of the diff.

Write NONE when the change has no effect a user would notice: tests, refactors,
repo tooling, or documentation corrections.
-->

```release-note

```
