{{- include "aster.validateAnalysisRuntime" . -}}
{{- if and (eq .Values.analysisRuntime.type "orka-container") (not .Values.analysisRuntime.orkaContainer.namespace) }}
apiVersion: v1
kind: Namespace
metadata:
  name: {{ include "aster.orkaAnalysisNamespace" . }}
  annotations:
    helm.sh/resource-policy: keep
  labels:
    {{- include "aster.labels" . | nindent 4 }}
    app.kubernetes.io/component: orka-container-analysis
    prow-ai-dashboard.willieyao.dev/release-scope: {{ include "aster.orkaReleaseScope" . | quote }}
{{- end }}
